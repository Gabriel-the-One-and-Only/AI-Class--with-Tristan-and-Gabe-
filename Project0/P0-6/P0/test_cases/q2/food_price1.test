class: "EvalTest"
success: "groceryShop correctly computes the cost of the order"
failure: "groceryShop must compute the correct cost of the order"

# A python expression to be evaluated.  This expression must return the 
# same result for the student and instructor's code.
test: "groceryShop.groceryShop([ ('apples', 4.0), ('celery',2.0), ('carrot',3.0) ])"
